name: CI
on:
  push:
    branches:
      - develop
  pull_request:

jobs:

  build-library:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup
        uses: ./.github/actions/setup

      - name: Build package
        run: yarn packCordova
